syntax = "proto3";
package crm.types.inventory;

option go_package = "github.com/kitdoo/my-business-crm-go/proto/gen/go/types/inventory;inventorypb";

// Read-only — see InventoryMovementsService for writes.
// Unique per (skuId, warehouseId).
message Inventory {
  string id = 1;
  int64 updatedAt = 2;
  string skuId = 3;
  string warehouseId = 4;
  // Hundredths of a unit (basis-points convention, same as
  // ProductPrice.priceAmount) — 216 means 2.16 units in stock.
  int64 quantity = 5;
  string etag = 101;
}
