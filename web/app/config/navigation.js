// Right-side menu (TD §8.2): one flat list — Dashboard plus every
// permission-visible entity from the registry, no intermediate section
// step. New entities show up here automatically once registered in
// config/entities/index.js — nothing to edit here when adding one.
export const NAV_ITEMS = [{ key: 'dashboard', label: 'nav.dashboard', icon: 'i-lucide-layout-dashboard', to: '/' }]
