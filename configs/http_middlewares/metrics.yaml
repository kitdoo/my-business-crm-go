# Configuration for the HTTP metrics collection middleware.
# Collects request counts, durations, and sizes for monitoring dashboards.
# Wires into the metrics.Collector configured at service init time via
# ServerBuilder.UseCollector.

#metrics:
#  # <bool> Enable metrics collection.
#  # Default value: false
#  enabled: false
#
#  # <list[string]> List of exact URL paths to skip processing for.
#  # Default value: not set
#  ignorePaths:
#    - "/health"
#
#  # <list[string]> List of regex patterns for URL paths to skip processing.
#  # Default value: not set
#  ignorePatterns:
#    - "^/health\\..*"
#
#  # <string> Metric subsystem name.
#  # Used as the second part of metric names after the collector's service
#  # name (e.g., "api" -> "<service>_api_server_requests_total"). Override
#  # per process-local upstream when several HTTP servers share one
#  # collector to keep metric namespaces distinct.
#  # Default value: not set (empty string)
#  subsystem: ""
#
#  # <bool> Collect request and response size metrics.
#  # Default value: false
#  enableSizeMetrics: false
#
#  # <list[float64]> Custom histogram buckets for request duration metrics in seconds.
#  # Must be in increasing order. If not specified, reasonable defaults are used.
#  # Default value: not set
#  durationBuckets:
#    - 0.001
#    - 0.01
#    - 0.1
#    - 1.0
#    - 10.0
#
#  # <list[float64]> Custom histogram buckets for request/response size metrics in bytes.
#  # Only used when enableSizeMetrics is true. Must be in increasing order.
#  # Default value: not set
#  sizeBuckets:
#    - 64
#    - 1024
#    - 16384
#    - 262144
#    - 1048576
